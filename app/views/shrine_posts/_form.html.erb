<%= form_with(model: shrine_post, local: true) do |form| %>
  <% if shrine_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shrine_post.errors.count, "error") %> prohibited this shrine_post from being saved:</h2>

      <ul>
      <% shrine_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="form-group">
    <%= form.label :description %>
    <%= form.text_area :description, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
